name: Compatibility

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@03b94c328c99fb628465841debd7ba83ca9b517f # v8.10.1
    with:
      go-versions: '["1.26","1.27"]'
