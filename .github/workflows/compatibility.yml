name: Compatibility

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions: {}

jobs:
  compatibility:
    permissions:
      contents: read
    uses: iwamot/workflows/.github/workflows/compatibility-go.yml@866cf6089ddb88af6b479a14a4a78910b472b5f6 # v8.11.0
    with:
      go-versions: '["1.26","1.27"]'
